# {{TARGET_NAME}}

Harmony Design System target: **{{TARGET_ID}}** (external).

> **Status: placeholder** — sync not yet implemented.

## Host

This target lives entirely in **{{HOST_SYSTEM}}**. There is no local component project.

Configuration: [`external.config.json`](external.config.json)

## Sync (future)

Sync will use the host system's tools (e.g. Figma MCP) to mirror foundation, shell, and components from the Astro reference.

See `converter.manifest.json` for converter readiness.

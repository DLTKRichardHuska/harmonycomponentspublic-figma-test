# Harmony {{TARGET_NAME}} — Consumer Guide

> **Placeholder** — fill in when sync implementation begins.

This target implements Harmony using **{{FRAMEWORK}}**, not Astro.

## Installation (future)

```bash
npm install {{PACKAGE_NAME}}
```

## Usage (future)

Document imports for this target's component API here (e.g. React + MUI), not Astro import paths.

## Demo site

Run `npm run dev` in the target project root to browse the reference/demo site for this implementation.

## Sync status

See `conversion.manifest.json` for per-element sync status (foundation, shell, components).

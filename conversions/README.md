# Conversions

Output of converter agent work — runnable projects (component-library) or external host data.

State lives in **`conversion.manifest.json`** per conversion (all element status, strategy, gaps).

Discover conversions: scan `conversions/*/conversion.manifest.json`.

| Conversion | Converter |
|------------|-----------|
| `harmony-design-system-react-mui` | `harmony-design-system-react-mui` |

Entry point: **`/conversion-agent`**

/* Generated by scripts/generate-styles.mjs from src/styles/reset.css + typography.css — do not edit. */
export const typographyCss = `/**
 * Harmony CSS reset — port of reference \`src/styles/reset.css\`.
 *
 * Fonts are not bundled: the host loads Figtree, Lexend, and JetBrains Mono.
 * Typography style sizes (h1 = Heading XL, etc.) live in typography.css and
 * override the generic heading/body rules here.
 *
 * Prepended to product styles.css by build-product.mjs (before typography),
 * and included in \`typographySheet\` for Shadow DOM.
 */

/* ===========================================
   BOX SIZING
   =========================================== */

*,
*::before,
*::after {
  box-sizing: border-box;
}

/* ===========================================
   MARGINS & PADDING RESET
   =========================================== */

/* Enumerated rather than \`*\` (as in the reference): a universal margin/padding
   rule in the document tree also matches custom elements, and normal document
   declarations beat a shadow root's own \`:host\` rules — so \`*\` silently zeroes
   the padding every Harmony element sets on its host. */
:where(
  address, blockquote, body, button, caption, dd, details, dialog, dl, dt,
  fieldset, figure, form, h1, h2, h3, h4, h5, h6, hr, input, legend, li, menu,
  ol, optgroup, option, p, pre, search, select, summary, table, td, textarea,
  th, ul
) {
  margin: 0;
  padding: 0;
}

/* ===========================================
   ROOT & BODY
   =========================================== */

html {
  scroll-behavior: smooth;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

body {
  font-family: var(--font-sans);
  font-size: var(--body-default);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
  color: var(--text-primary);
  background-color: var(--page-bg);
  min-height: 100vh;
}

/* ===========================================
   TYPOGRAPHY (generic — type styles override in typography.css)
   =========================================== */

h1,
h2,
h3,
h4,
h5,
h6 {
  font-family: var(--font-display);
  color: var(--text-primary);
}

p {
  color: var(--text-secondary);
}

/* ===========================================
   LINKS
   =========================================== */

a {
  color: var(--link-color);
  text-decoration: none;
  transition: color var(--transition-fast);
}

a:hover {
  text-decoration: underline;
}

/* ===========================================
   LISTS
   =========================================== */

/* Lists keep their markers (the reference resets them away). A component that
   uses a list for structure rather than for content — nav, tab strip, menu —
   opts out with \`list-style: none\` in its own styles. */
ul {
  list-style-type: disc;
}

ol {
  list-style-type: decimal;
}

/* Markers sit outside the content box, so the indent has to come back after
   the margin/padding reset above. */
ul,
ol {
  padding-inline-start: var(--space-6);
}

/* ===========================================
   FIELDSET & LEGEND
   =========================================== */

fieldset {
  border: none;
  padding: 0;
  margin: 0;
  min-width: 0;
}

legend {
  padding: 0;
  font-size: var(--text-sm);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
  margin-bottom: var(--space-2);
}

/* ===========================================
   IMAGES & MEDIA
   =========================================== */

img,
picture,
video,
canvas,
svg {
  display: block;
  max-width: 100%;
  height: auto;
}

/* ===========================================
   FORM ELEMENTS
   =========================================== */

input,
button,
textarea,
select {
  font: inherit;
  color: inherit;
}

button {
  cursor: pointer;
  background: none;
  border: none;
}

input:focus,
textarea:focus,
select:focus,
button:focus {
  outline: none;
}

input[type='number']::-webkit-inner-spin-button,
input[type='number']::-webkit-outer-spin-button {
  -webkit-appearance: none;
  margin: 0;
}

input[type='number'] {
  -moz-appearance: textfield;
}

input[type='date'],
input[type='time'],
input[type='datetime-local'],
input[type='month'],
input[type='week'] {
  -webkit-appearance: none;
  appearance: none;
}

html:not(.dark) input[type='date'],
html:not(.dark) input[type='time'],
html:not(.dark) input[type='datetime-local'],
html:not(.dark) input[type='month'],
html:not(.dark) input[type='week'] {
  color-scheme: light;
}

html.dark input[type='date'],
html.dark input[type='time'],
html.dark input[type='datetime-local'],
html.dark input[type='month'],
html.dark input[type='week'] {
  color-scheme: dark;
}

input[type='color'] {
  -webkit-appearance: none;
  appearance: none;
  padding: 0;
  border: none;
  cursor: pointer;
}

input[type='color']::-webkit-color-swatch-wrapper {
  padding: 0;
}

input[type='color']::-webkit-color-swatch {
  border: none;
  border-radius: var(--radius-lg);
}

input:disabled,
textarea:disabled,
select:disabled,
button:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

/* ===========================================
   TABLE
   =========================================== */

table {
  border-collapse: collapse;
  border-spacing: 0;
  width: 100%;
}

/* ===========================================
   CODE
   =========================================== */

code,
pre,
kbd,
samp {
  font-family: var(--font-mono);
  font-size: var(--text-sm);
}

pre {
  overflow-x: auto;
  background-color: var(--surface-bg);
  border: var(--border-width-thin) solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: var(--space-4);
}

code {
  background-color: var(--elevated-bg);
  padding: var(--space-0-5) var(--space-1-5);
  border-radius: var(--radius-sm);
  color: var(--text-primary);
}

pre code {
  background: none;
  padding: 0;
  border-radius: 0;
  /* Take the block's color so a themed \`pre\` is not overridden by the inline
     \`code\` color above. */
  color: inherit;
}

/* ===========================================
   SELECTION
   =========================================== */

::selection {
  background-color: var(--theme-primary-light);
  color: var(--text-primary);
}

/* ===========================================
   SCROLLBAR
   =========================================== */

::-webkit-scrollbar {
  width: var(--space-1-5);
  height: var(--space-1-5);
}

::-webkit-scrollbar-track {
  background: var(--scrollbar-track);
}

::-webkit-scrollbar-thumb {
  background: var(--scrollbar-thumb);
  border-radius: var(--radius-03);
}

::-webkit-scrollbar-thumb:hover {
  background: var(--scrollbar-thumb-hover);
}

* {
  scrollbar-width: thin;
  scrollbar-color: var(--scrollbar-thumb) var(--scrollbar-track);
}

/* ===========================================
   FOCUS VISIBLE
   =========================================== */

:focus-visible {
  outline: var(--border-width-standard) solid var(--theme-primary);
  outline-offset: var(--space-0-5);
}

/* ===========================================
   HIDDEN UTILITY
   =========================================== */

[hidden] {
  display: none !important;
}

/* ===========================================
   SCREEN READER ONLY
   =========================================== */

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}

/**
 * Harmony typography — element defaults + type-style classes.
 *
 * Type-style and utility classes port \`src/styles/utilities.css\`. Tag defaults
 * (heading-first) override the generic heading/body rules in reset.css.
 *
 * Display styles are opt-in via \`.text-display-*\` so hero type never lands
 * on a page by accident.
 *
 * Appended after reset.css in product styles.css, and included in
 * \`typographySheet\` for Shadow DOM.
 */

/* ===========================================
   ELEMENT DEFAULTS (heading-first type styles)
   =========================================== */

body {
  font-family: var(--font-sans);
  font-size: var(--body-default);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
  color: var(--text-primary);
  background-color: var(--page-bg);
}

h1,
h2,
h3,
h4,
h5,
h6 {
  font-family: var(--font-display);
  color: var(--text-primary);
}

h1 {
  font-size: var(--heading-xl);
  font-weight: var(--font-semibold);
  line-height: var(--leading-snug);
}

h2 {
  font-size: var(--heading-l);
  font-weight: var(--font-semibold);
  line-height: var(--leading-snug);
}

h3 {
  font-size: var(--heading-m);
  font-weight: var(--font-semibold);
  line-height: var(--leading-snug);
}

h4,
h5,
h6 {
  font-size: var(--heading-s);
  font-weight: var(--font-medium);
  line-height: var(--leading-snug);
}

p {
  font-family: var(--font-sans);
  font-size: var(--body-default);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
  color: var(--text-secondary);
}

label {
  font-family: var(--font-display);
  font-size: var(--label);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
}

small {
  font-family: var(--font-sans);
  font-size: var(--caption);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
}

strong,
b {
  font-weight: var(--font-semibold);
}

code,
pre,
kbd,
samp {
  font-family: var(--font-mono);
  font-size: var(--text-sm);
}

/* ===========================================
   TYPE SCALE - DISPLAY
   =========================================== */

.text-display-xl {
  font-size: var(--display-xl);
  font-family: var(--font-display);
  font-weight: var(--font-bold);
  line-height: var(--leading-tight);
}
.text-display-l {
  font-size: var(--display-l);
  font-family: var(--font-display);
  font-weight: var(--font-bold);
  line-height: var(--leading-tight);
}
.text-display-m {
  font-size: var(--display-m);
  font-family: var(--font-display);
  font-weight: var(--font-bold);
  line-height: var(--leading-tight);
}

/* ===========================================
   TYPE SCALE - HEADINGS
   =========================================== */

.text-heading-xl {
  font-size: var(--heading-xl);
  font-family: var(--font-display);
  font-weight: var(--font-semibold);
  line-height: var(--leading-snug);
}
.text-heading-l {
  font-size: var(--heading-l);
  font-family: var(--font-display);
  font-weight: var(--font-semibold);
  line-height: var(--leading-snug);
}
.text-heading-m {
  font-size: var(--heading-m);
  font-family: var(--font-display);
  font-weight: var(--font-semibold);
  line-height: var(--leading-snug);
}
.text-heading-s {
  font-size: var(--heading-s);
  font-family: var(--font-display);
  font-weight: var(--font-medium);
  line-height: var(--leading-snug);
}

/* ===========================================
   TYPE SCALE - BODY
   =========================================== */

.text-body-default {
  font-size: var(--body-default);
  font-family: var(--font-sans);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
}
.text-body-emphasized {
  font-size: var(--body-emphasized);
  font-family: var(--font-sans);
  font-weight: var(--font-semibold);
  line-height: var(--leading-normal);
}

/* ===========================================
   TYPE SCALE - SUPPORTING
   =========================================== */

.text-caption {
  font-size: var(--caption);
  font-family: var(--font-sans);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
}
.text-label {
  font-size: var(--label);
  font-family: var(--font-display);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
}
.text-overline {
  font-size: var(--overline);
  font-family: var(--font-sans);
  font-weight: var(--font-semibold);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  line-height: var(--leading-normal);
}

/* ===========================================
   FONT FAMILY
   =========================================== */

.font-sans {
  font-family: var(--font-sans);
}
.font-display {
  font-family: var(--font-display);
}
.font-mono {
  font-family: var(--font-mono);
}

/* ===========================================
   FONT SIZE
   =========================================== */

.text-xs {
  font-size: var(--text-xs);
}
.text-sm {
  font-size: var(--text-sm);
}
.text-base {
  font-size: var(--text-base);
}
.text-lg {
  font-size: var(--text-lg);
}
.text-xl {
  font-size: var(--text-xl);
}
.text-2xl {
  font-size: var(--text-2xl);
}
.text-3xl {
  font-size: var(--text-3xl);
}
.text-4xl {
  font-size: var(--text-4xl);
}
.text-5xl {
  font-size: var(--text-5xl);
}

/* ===========================================
   FONT WEIGHT
   =========================================== */

.font-light {
  font-weight: var(--font-light);
}
.font-normal {
  font-weight: var(--font-normal);
}
.font-medium {
  font-weight: var(--font-medium);
}
.font-semibold {
  font-weight: var(--font-semibold);
}
.font-bold {
  font-weight: var(--font-bold);
}

/* ===========================================
   LINE HEIGHT
   =========================================== */

.leading-none {
  line-height: var(--leading-none);
}
.leading-tight {
  line-height: var(--leading-tight);
}
.leading-normal {
  line-height: var(--leading-normal);
}
.leading-relaxed {
  line-height: var(--leading-relaxed);
}
`;

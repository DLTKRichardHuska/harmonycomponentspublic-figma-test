/* Generated by scripts/generate-styles.mjs from src/styles/pickers.css — do not edit. */
export const pickersCss = `/**
 * Date/time picker widgets — shadow styles for harmony-picker-popup,
 * harmony-date-picker, harmony-time-picker, harmony-month-picker, harmony-week-picker.
 * Port of reference picker CSS without range-day modifiers (DateRange is out of scope).
 */

:host {
  display: block;
  box-sizing: border-box;
}

/* ===========================================
   PICKER POPUP
   =========================================== */

:host(harmony-picker-popup) {
  display: none;
  margin: 0;
  padding: 0;
  border: none;
  background: transparent;
  inset: auto;
  width: max-content;
  max-width: min(320px, calc(100vw - 32px));
}

:host(harmony-picker-popup:popover-open) {
  display: block;
}

@supports (top: anchor(bottom)) {
  :host(harmony-picker-popup:popover-open):not([data-anchor='fallback']) {
    margin: 0;
    inset: unset;
    top: anchor(bottom);
    left: anchor(left);
    margin-top: var(--space-2);
    position-try-fallbacks: flip-block;
  }
}

:host(harmony-picker-popup[data-anchor='fallback']:popover-open) {
  position: fixed;
}

[part='panel'] {
  background-color: var(--card-bg);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-xl);
  padding: var(--space-4);
  min-width: 280px;
  max-width: 320px;
}

.picker-popup__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-4);
  padding-bottom: var(--space-3);
  border-bottom: 1px solid var(--border-light);
}

.picker-popup__title {
  font-size: var(--text-base);
  font-weight: var(--font-semibold);
  font-family: var(--font-sans);
  color: var(--text-primary);
  margin: 0;
}

.picker-popup__close {
  display: flex;
  align-items: center;
  justify-content: center;
  width: var(--space-8);
  height: var(--space-8);
  padding: 0;
  background: transparent;
  border: none;
  border-radius: var(--radius-md);
  color: var(--text-secondary);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.picker-popup__close:hover {
  background-color: var(--hover-bg);
  color: var(--text-primary);
}

.picker-popup__close:focus-visible {
  outline: 2px solid var(--theme-primary);
  outline-offset: 2px;
}

@media (max-width: 768px) {
  :host(harmony-picker-popup:popover-open) {
    position: fixed;
    inset: 50% auto auto 50%;
    top: 50%;
    left: 50%;
    right: auto;
    transform: translate(-50%, -50%);
    width: calc(100vw - 32px);
    max-width: calc(100vw - 32px);
    margin: 0;
  }

  [part='panel'] {
    width: 100%;
    max-width: none;
    min-width: 0;
    max-height: calc(100vh - 100px);
    overflow-y: auto;
  }

  .picker-popup__close {
    min-width: 44px;
    min-height: 44px;
  }
}

/* ===========================================
   DATE PICKER
   =========================================== */

.date-picker {
  width: 100%;
}

.date-picker__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-4);
}

.date-picker__nav-btn,
.month-picker__nav-btn,
.week-picker__nav-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: var(--space-8);
  height: var(--space-8);
  padding: 0;
  background: transparent;
  border: 1px solid var(--border-color);
  border-radius: var(--radius-md);
  color: var(--text-secondary);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.date-picker__nav-btn:hover:not(:disabled),
.month-picker__nav-btn:hover:not(:disabled),
.week-picker__nav-btn:hover:not(:disabled) {
  background-color: var(--hover-bg);
  color: var(--text-primary);
  border-color: var(--border-color);
}

.date-picker__nav-btn:disabled,
.month-picker__nav-btn:disabled,
.week-picker__nav-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.date-picker__nav-btn:focus-visible,
.month-picker__nav-btn:focus-visible,
.week-picker__nav-btn:focus-visible {
  outline: 2px solid var(--theme-primary);
  outline-offset: 2px;
}

.date-picker__month-year {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-family: var(--font-display);
  font-size: var(--text-base);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
}

.date-picker__weekdays {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: var(--space-1);
  margin-bottom: var(--space-2);
}

.date-picker__weekday {
  display: flex;
  align-items: center;
  justify-content: center;
  height: var(--space-8);
  font-size: var(--text-xs);
  font-weight: var(--font-medium);
  color: var(--text-muted);
  text-align: center;
}

.date-picker__grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: var(--space-1);
}

.date-picker__day {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  padding: 0;
  font-size: var(--text-sm);
  font-family: var(--font-sans);
  color: var(--text-primary);
  background: transparent;
  border: 1px solid transparent;
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.date-picker__day:hover:not(:disabled) {
  background-color: var(--hover-bg);
  border-color: var(--border-color);
}

.date-picker__day--today {
  font-weight: var(--font-semibold);
  border-color: var(--theme-primary);
}

.date-picker__day--selected {
  background-color: var(--theme-primary);
  color: var(--text-inverse);
  border-color: var(--theme-primary);
  font-weight: var(--font-semibold);
}

.date-picker__day--selected:hover:not(:disabled) {
  background-color: var(--theme-primary-hover);
  border-color: var(--theme-primary-hover);
}

.date-picker__day--other-month {
  color: var(--text-muted);
  opacity: 0.5;
}

.date-picker__day--disabled {
  opacity: 0.4;
  cursor: not-allowed;
  color: var(--text-muted);
}

.date-picker__day--disabled:hover {
  background: transparent;
  border-color: transparent;
}

.date-picker__day:focus {
  outline: none;
  box-shadow: var(--focus-ring-date-picker);
}

.date-picker__day:focus-visible {
  outline: 2px solid var(--theme-primary);
  outline-offset: 1px;
}

/* ===========================================
   TIME PICKER
   =========================================== */

.time-picker {
  width: 100%;
}

.time-picker__controls {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
}

.time-picker__hour,
.time-picker__minute {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-1);
}

.time-picker__increment,
.time-picker__decrement {
  display: flex;
  align-items: center;
  justify-content: center;
  width: var(--space-8);
  height: var(--space-6);
  padding: 0;
  background: transparent;
  border: 1px solid var(--border-color);
  border-radius: var(--radius-md);
  color: var(--text-secondary);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.time-picker__increment:hover:not(:disabled),
.time-picker__decrement:hover:not(:disabled) {
  background-color: var(--hover-bg);
  color: var(--text-primary);
  border-color: var(--border-color);
}

.time-picker__increment:disabled,
.time-picker__decrement:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.time-picker__increment:focus-visible,
.time-picker__decrement:focus-visible {
  outline: 2px solid var(--theme-primary);
  outline-offset: 2px;
}

.time-picker__input {
  width: var(--space-12);
  height: var(--space-10);
  padding: 0 var(--space-2);
  font-size: var(--text-lg);
  font-weight: var(--font-semibold);
  font-family: var(--font-sans);
  text-align: center;
  color: var(--text-primary);
  background-color: var(--input-bg);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-md);
  cursor: default;
}

.time-picker__input:focus {
  outline: none;
  border-color: var(--theme-primary);
  box-shadow: var(--focus-ring-primary);
}

.time-picker__separator {
  font-size: var(--text-lg);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
  padding: 0 var(--space-1);
}

.time-picker__period {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
  margin-left: var(--space-2);
}

.time-picker__period-btn {
  width: var(--space-12);
  height: var(--space-8);
  padding: 0 var(--space-2);
  font-size: var(--text-sm);
  font-weight: var(--font-medium);
  font-family: var(--font-sans);
  color: var(--text-secondary);
  background-color: var(--input-bg);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.time-picker__period-btn:hover:not(:disabled) {
  background-color: var(--hover-bg);
  color: var(--text-primary);
  border-color: var(--border-color);
}

.time-picker__period-btn--active {
  background-color: var(--theme-primary);
  color: var(--text-inverse);
  border-color: var(--theme-primary);
  font-weight: var(--font-semibold);
}

.time-picker__period-btn--active:hover:not(:disabled) {
  background-color: var(--theme-primary-hover);
  border-color: var(--theme-primary-hover);
}

.time-picker__period-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.time-picker__period-btn:focus-visible {
  outline: 2px solid var(--theme-primary);
  outline-offset: 2px;
}

/* ===========================================
   MONTH PICKER
   =========================================== */

.month-picker {
  width: 100%;
}

.month-picker__header,
.week-picker__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-4);
}

.month-picker__year,
.week-picker__year {
  font-family: var(--font-display);
  font-size: var(--text-base);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
}

.month-picker__grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-2);
}

.month-picker__month {
  display: flex;
  align-items: center;
  justify-content: center;
  height: var(--space-10);
  padding: 0 var(--space-3);
  font-size: var(--text-sm);
  font-family: var(--font-sans);
  color: var(--text-primary);
  background: transparent;
  border: 1px solid var(--border-color);
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.month-picker__month:hover:not(:disabled) {
  background-color: var(--hover-bg);
  border-color: var(--border-color);
}

.month-picker__month--selected {
  background-color: var(--theme-primary);
  color: var(--text-inverse);
  border-color: var(--theme-primary);
  font-weight: var(--font-semibold);
}

.month-picker__month--selected:hover:not(:disabled) {
  background-color: var(--theme-primary-hover);
  border-color: var(--theme-primary-hover);
}

.month-picker__month--disabled,
.week-picker__week--disabled {
  opacity: 0.4;
  cursor: not-allowed;
  color: var(--text-muted);
}

.month-picker__month--disabled:hover {
  background: transparent;
  border-color: var(--border-color);
}

.month-picker__month:focus,
.week-picker__week:focus {
  outline: none;
  box-shadow: var(--focus-ring-date-picker);
}

.month-picker__month:focus-visible,
.week-picker__week:focus-visible {
  outline: 2px solid var(--theme-primary);
  outline-offset: 1px;
}

/* ===========================================
   WEEK PICKER
   =========================================== */

.week-picker {
  width: 100%;
  max-height: 400px;
  overflow-y: auto;
}

.week-picker__header {
  padding-bottom: var(--space-3);
  border-bottom: 1px solid var(--border-light);
}

.week-picker__list {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}

.week-picker__week {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  padding: var(--space-2) var(--space-3);
  font-size: var(--text-sm);
  font-family: var(--font-sans);
  text-align: left;
  color: var(--text-primary);
  background: transparent;
  border: 1px solid transparent;
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.week-picker__week:hover:not(:disabled) {
  background-color: var(--hover-bg);
  border-color: var(--border-color);
}

.week-picker__week--disabled:hover {
  background: transparent;
  border-color: transparent;
}

.week-picker__week--selected {
  background-color: var(--theme-primary);
  color: var(--text-inverse);
  border-color: var(--theme-primary);
  font-weight: var(--font-semibold);
}

.week-picker__week--selected:hover:not(:disabled) {
  background-color: var(--theme-primary-hover);
  border-color: var(--theme-primary-hover);
}

.week-picker__week-number {
  font-weight: var(--font-medium);
}

.week-picker__week-range {
  font-size: var(--text-xs);
  color: var(--text-secondary);
}

.week-picker__week--selected .week-picker__week-range {
  color: var(--text-inverse);
  opacity: 0.9;
}

/* ===========================================
   FORCED COLORS
   =========================================== */

/* ===========================================
   DATETIME PICKER
   =========================================== */

.datetime-picker {
  width: 100%;
}

.datetime-picker__date-section {
  margin-bottom: var(--space-4);
  padding-bottom: var(--space-4);
  border-bottom: 1px solid var(--border-light);
}

@media (max-width: 768px) {
  .datetime-picker {
    width: 100%;
  }

  .datetime-picker__date-section {
    margin-bottom: var(--space-3);
    padding-bottom: var(--space-3);
  }
}

@media (forced-colors: active) {
  [part='panel'] {
    background-color: Canvas;
    color: CanvasText;
    border: 1px solid CanvasText;
    box-shadow: none;
  }

  .picker-popup__header,
  .week-picker__header {
    border-bottom-color: CanvasText;
  }

  .picker-popup__title,
  .date-picker__month-year,
  .month-picker__year,
  .week-picker__year,
  .time-picker__separator {
    color: CanvasText;
  }

  .picker-popup__close,
  .date-picker__nav-btn,
  .month-picker__nav-btn,
  .week-picker__nav-btn,
  .time-picker__increment,
  .time-picker__decrement,
  .time-picker__period-btn,
  .date-picker__day,
  .month-picker__month,
  .week-picker__week,
  .time-picker__input {
    background-color: Canvas;
    color: CanvasText;
    border-color: CanvasText;
    box-shadow: none;
    forced-color-adjust: none;
  }

  .date-picker__day--selected,
  .month-picker__month--selected,
  .week-picker__week--selected,
  .time-picker__period-btn--active {
    background-color: Highlight;
    color: HighlightText;
    border-color: Highlight;
  }

  .week-picker__week--selected .week-picker__week-range {
    color: HighlightText;
  }

  .date-picker__day--disabled,
  .month-picker__month--disabled,
  .week-picker__week--disabled,
  .date-picker__nav-btn:disabled,
  .month-picker__nav-btn:disabled,
  .week-picker__nav-btn:disabled,
  .time-picker__increment:disabled,
  .time-picker__decrement:disabled,
  .time-picker__period-btn:disabled {
    color: GrayText;
    border-color: GrayText;
    opacity: 1;
  }

  .date-picker__day:focus-visible,
  .month-picker__month:focus-visible,
  .week-picker__week:focus-visible,
  .date-picker__nav-btn:focus-visible,
  .month-picker__nav-btn:focus-visible,
  .week-picker__nav-btn:focus-visible,
  .time-picker__increment:focus-visible,
  .time-picker__decrement:focus-visible,
  .time-picker__period-btn:focus-visible,
  .time-picker__input:focus-visible,
  .picker-popup__close:focus-visible {
    outline: 2px solid Highlight;
    outline-offset: 2px;
    box-shadow: none;
  }

  .datetime-picker__date-section {
    border-bottom-color: CanvasText;
  }
}

/* ===========================================
   DATE INPUT FIELD (harmony-date-input)
   Field chrome only. Sized with --harmony-field-* so product flatten
   (CP 20px) applies. Icon uses the input trailing position, not the
   CP-only offset. Skip .theme-cp selectors.
   =========================================== */

:host(harmony-date-input) {
  display: block;
  width: 100%;
  box-sizing: border-box;
  font-family: var(--font-sans);
}

:host(harmony-date-input[hidden]) {
  display: none;
}

:host(harmony-date-input) [part='label'] {
  display: block;
  font-family: var(--font-display);
  font-size: var(--text-sm);
  font-weight: var(--font-normal);
  color: var(--text-primary);
  margin-bottom: 0;
}

:host(harmony-date-input) [part='label'][hidden] {
  display: none;
}

:host(harmony-date-input) [part='label'].label--required::after {
  content: ' *';
  color: var(--color-error);
}

:host(harmony-date-input[data-in-form-layout]) [part='label'] {
  display: none;
}

:host(harmony-date-input) .field {
  display: flex;
  flex-direction: column;
  gap: var(--space-1-5);
  width: 100%;
}

:host(harmony-date-input) .field__body {
  display: flex;
  flex-direction: column;
  flex: 1 1 auto;
  min-width: 0;
  width: 100%;
}

:host(harmony-date-input[data-label-variant='inline']) .field {
  flex-direction: row;
  align-items: center;
  gap: var(--space-1);
}

:host(harmony-date-input[data-label-variant='inline']) [part='label'] {
  margin-bottom: 0;
  white-space: nowrap;
}

:host(harmony-date-input[data-in-form-layout]) .field {
  flex-direction: column;
  align-items: stretch;
  gap: var(--space-1-5);
}

.date-input-wrapper {
  position: relative;
  width: 100%;
}

[part='field'].date-input {
  box-sizing: border-box;
  width: 100%;
  height: var(--harmony-field-height, var(--space-10));
  padding: 0 var(--harmony-field-pad-x, var(--space-4));
  padding-right: var(--harmony-field-icon-gutter, var(--space-10));
  font-family: var(--font-sans);
  font-size: var(--harmony-field-font-size, var(--text-base));
  line-height: var(--harmony-field-height, var(--space-10));
  color: var(--text-primary);
  background-color: var(--input-bg);
  border: 1px solid var(--border-color);
  border-radius: var(--harmony-field-radius, var(--radius-lg));
  cursor: pointer;
  transition: border-color var(--transition-fast), box-shadow var(--transition-fast),
    background-color var(--transition-fast);
  appearance: none;
  -webkit-appearance: none;
  margin: 0;
}

.date-input::-webkit-calendar-picker-indicator {
  display: none;
  -webkit-appearance: none;
}

.date-input::-webkit-inner-spin-button,
.date-input::-webkit-outer-spin-button {
  display: none;
  -webkit-appearance: none;
}

.date-input::-moz-calendar-picker-indicator {
  display: none;
}

.date-input:focus,
.date-input:focus-visible {
  outline: none;
  border-color: var(--theme-primary);
  box-shadow: var(--focus-ring-primary);
}

.date-input:disabled {
  background-color: var(--input-disabled-bg);
  color: var(--text-muted);
  cursor: not-allowed;
}

.date-input::placeholder {
  color: var(--text-muted);
}

[part='icon'].date-input-wrapper__icon {
  position: absolute;
  right: calc(
    1px + (var(--harmony-field-icon-gutter, var(--space-10)) - 1px - var(--icon-sm)) / 2
  );
  top: var(--harmony-field-icon-top, 50%);
  transform: translateY(-50%);
  color: var(--text-secondary);
  background: transparent;
  border: none;
  padding: 0;
  margin: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  z-index: var(--z-10);
}

[part='icon'].date-input-wrapper__icon:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

[part='icon'] harmony-icon {
  color: inherit;
  pointer-events: none;
}

@media (max-width: 768px) {
  .date-input {
    width: 100%;
    font-size: 16px;
    min-height: 44px;
  }
}

@media (forced-colors: active) {
  :host(harmony-date-input) [part='field'] {
    border: 1px solid CanvasText;
    background-color: Canvas;
    color: CanvasText;
    forced-color-adjust: none;
  }

  :host(harmony-date-input) [part='field']:focus,
  :host(harmony-date-input) [part='field']:focus-visible {
    outline: 2px solid Highlight;
    outline-offset: 2px;
    box-shadow: none;
    border-color: Highlight;
  }

  :host(harmony-date-input) [part='field']:disabled {
    color: GrayText;
    border-color: GrayText;
    background-color: Canvas;
  }

  :host(harmony-date-input) [part='icon'] {
    color: CanvasText;
    opacity: 1;
    forced-color-adjust: none;
  }

  :host(harmony-date-input) [part='icon']:disabled {
    color: GrayText;
    border: 1px solid GrayText;
    opacity: 1;
  }

  :host(harmony-date-input) [part='label'] {
    color: CanvasText;
  }

  :host(harmony-date-input) [part='label'].label--required::after {
    color: CanvasText;
  }
}
`;

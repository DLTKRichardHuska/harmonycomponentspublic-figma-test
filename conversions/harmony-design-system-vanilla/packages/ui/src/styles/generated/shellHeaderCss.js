/* Generated by scripts/generate-styles.mjs from src/styles/shell-header.css — do not edit. */
export const shellHeaderCss = `/**
 * Shell header — Shadow DOM recipe for \`<harmony-shell-header>\`.
 */

:host {
  display: block;
  box-sizing: border-box;
  width: 100%;
  height: var(--shell-header-height, 56px);
  background-color: var(--nav-bg);
  border-bottom: none;
  position: relative;
  flex-shrink: 0;
  color: var(--text-primary);
  font-family: var(--font-sans);
}

.header {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 var(--space-4);
  box-sizing: border-box;
}

@media (min-width: 640px) {
  .header {
    padding: 0 var(--space-6);
  }
}

.header__brand {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  min-width: 0;
}

.header__brand-link {
  display: inline-flex;
  align-items: center;
  gap: var(--space-3);
  text-decoration: none;
  color: inherit;
  min-width: 0;
}

.header__brand-link:hover {
  opacity: 0.9;
}

.header__logo {
  width: var(--space-5);
  height: var(--space-5);
  border-radius: var(--radius-full);
  flex-shrink: 0;
  display: inline-flex;
  overflow: hidden;
}

.header__logo svg {
  width: 100%;
  height: 100%;
  display: block;
}

.header__title {
  font-family: var(--font-display);
  font-weight: var(--font-bold);
  font-size: var(--text-sm);
  color: var(--text-primary);
  letter-spacing: -0.025em;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.header__actions {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  min-width: 0;
}

.header__gradient {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: var(--space-1-5);
  pointer-events: none;
  background: linear-gradient(
    to right,
    color-mix(in srgb, var(--header-gradient-color, var(--theme-primary)) 2%, transparent),
    var(--header-gradient-color, var(--theme-primary)) 50%,
    color-mix(in srgb, var(--header-gradient-color, var(--theme-primary)) 2%, transparent)
  );
}

@media (forced-colors: active) {
  :host {
    border-bottom: 1px solid CanvasText;
  }

  .header__gradient {
    forced-color-adjust: none;
    background: CanvasText;
    height: 2px;
    opacity: 0.5;
  }

  .header__brand-link:focus-visible {
    outline: 2px solid Highlight;
    outline-offset: 2px;
  }
}
`;

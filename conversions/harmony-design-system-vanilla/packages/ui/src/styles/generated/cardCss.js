/* Generated by scripts/generate-styles.mjs from src/styles/card.css — do not edit. */
export const cardCss = `/**
 * Harmony Card — native BEM recipe + light-DOM hybrid host
 *
 * Header actions: compose catalog Button (ghost icon-only xs).
 * Do not ship .card__icon-btn (Astro private chrome — accepted gap).
 */

.card {
  background-color: var(--card-bg);
  border: var(--border-width-thin) solid var(--border-color);
  border-radius: var(--radius-xl);
  overflow: hidden;
  box-shadow: var(--shadow-sm);
  display: block;
}

/* Whole-card links: neutralize default / host link chrome (same idea as .btn on <a>). */
a.card {
  color: inherit;
  text-decoration: none;
}

.card--elevated {
  box-shadow: var(--shadow-lg);
}

.card--interactive {
  cursor: pointer;
  transition: all var(--transition-fast);
}

.card--interactive:hover {
  border-color: var(--theme-primary);
  box-shadow: var(--shadow-lg);
}

.card--interactive:focus-visible {
  outline: var(--border-width-standard) solid var(--theme-primary);
  outline-offset: var(--space-0-5);
}

.card--primary {
  border-top: var(--space-1) solid var(--theme-primary);
}

.card__header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: var(--space-4) var(--space-4) 0 var(--space-4);
}

.card__header-content {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
  flex: 1;
}

.card__header-title {
  font-family: var(--font-display);
  font-size: var(--heading-s);
  font-weight: normal;
  line-height: var(--space-6);
  color: var(--text-primary);
  margin: 0;
}

.card__header-subtitle {
  font-family: var(--font-sans);
  font-size: var(--label);
  line-height: var(--space-5);
  color: var(--text-secondary);
  margin: 0;
}

.card__header-actions {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.card__title {
  font-family: var(--font-display);
  font-size: var(--text-lg);
  font-weight: normal;
  color: var(--text-primary);
}

.card__subtitle {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  margin-top: var(--space-1);
}

.card__body {
  padding: var(--space-4);
}

.card__footer {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-4);
  border-top: var(--border-width-thin) solid var(--border-color);
  background-color: var(--elevated-bg);
}

/* Forced-colors: keep boundaries and focus when shadows are suppressed */
@media (forced-colors: active) {
  .card {
    border: var(--border-width-thin) solid CanvasText;
    box-shadow: none;
    forced-color-adjust: none;
  }

  .card--elevated,
  .card--interactive:hover {
    box-shadow: none;
    border-color: CanvasText;
  }

  .card--primary {
    border-top-color: Highlight;
  }

  .card--interactive:focus-visible {
    outline: var(--border-width-standard) solid Highlight;
    outline-offset: var(--space-0-5);
  }

  .card__footer {
    border-top-color: CanvasText;
    background-color: Canvas;
  }
}
`;

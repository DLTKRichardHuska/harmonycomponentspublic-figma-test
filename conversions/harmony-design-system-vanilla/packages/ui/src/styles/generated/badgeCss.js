/* Generated by scripts/generate-styles.mjs from src/styles/badge.css — do not edit. */
export const badgeCss = `/**
 * Badge visual recipe — used by \`<harmony-badge>\` Shadow DOM.
 * Not appended to product document styles.css (CE-only surface).
 */

:host {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  box-sizing: border-box;
  padding: 0 var(--space-2);
  height: var(--badge-height-lg);
  min-height: var(--badge-height-lg);
  font-family: var(--font-display);
  font-size: 0.875rem;
  font-weight: var(--font-normal);
  border-radius: var(--radius-full);
  line-height: var(--leading-normal);
  background-color: var(--elevated-bg);
  color: var(--text-secondary);
  border: var(--border-width-thin) solid var(--border-color);
  vertical-align: middle;
}

:host([size='small']) {
  font-size: 0.625rem;
  height: var(--badge-height-sm);
  min-height: var(--badge-height-sm);
  padding: 0 var(--space-1-5);
  line-height: var(--leading-none);
}

:host([size='medium']) {
  font-size: var(--text-xs);
  height: var(--badge-height-md);
  min-height: var(--badge-height-md);
  padding: 0 var(--space-1-5);
  line-height: var(--leading-tight);
}

:host([size='large']) {
  font-size: 0.875rem;
  height: var(--badge-height-lg);
  min-height: var(--badge-height-lg);
  padding: 0 var(--space-2);
}

:host([variant='primary']) {
  background-color: var(--alert-chip-blue-bg);
  color: var(--alert-chip-blue-fg);
  border-color: var(--alert-chip-blue-fg);
}

:host([variant='error']) {
  background-color: var(--alert-chip-error-bg);
  color: var(--alert-chip-error-fg);
  border-color: var(--alert-chip-error-fg);
}

:host([variant='warning']) {
  background-color: var(--alert-chip-warning-bg);
  color: var(--alert-chip-warning-fg);
  border-color: var(--alert-chip-warning-fg);
}

:host([variant='success']) {
  background-color: var(--alert-chip-success-bg);
  color: var(--alert-chip-success-fg);
  border-color: var(--alert-chip-success-fg);
}

:host([variant='info']) {
  background-color: var(--alert-chip-info-bg);
  color: var(--alert-chip-info-fg);
  border-color: var(--alert-chip-info-fg);
}

:host([variant='orange']) {
  background-color: var(--alert-chip-orange-bg);
  color: var(--alert-chip-orange-fg);
  border-color: var(--alert-chip-orange-fg);
}

:host([variant='pink']) {
  background-color: var(--alert-chip-pink-bg);
  color: var(--alert-chip-pink-fg);
  border-color: var(--alert-chip-pink-fg);
}

:host([variant='disabled']) {
  background-color: var(--alert-chip-disabled-bg);
  color: var(--alert-chip-disabled-fg);
  border-color: var(--alert-chip-disabled-border);
}

[part='icon'] {
  display: inline-flex;
  flex-shrink: 0;
  color: inherit;
}

slot {
  display: inline;
}

@media (forced-colors: active) {
  :host {
    background-color: Canvas;
    color: CanvasText;
    border: var(--border-width-thin) solid CanvasText;
  }
  :host([variant='disabled']) {
    color: GrayText;
    border-color: GrayText;
  }
}
`;
